---
import Base from "../../../layouts/Base.astro";
import { getCollection } from "astro:content";

/* Featured flag normalization */
function isFeatured(post) {
  const v = post?.data?.featured;
  if (v === true) return true;
  if (v === 1) return true;
  const s = String(v ?? "").toLowerCase().trim();
  return s === "true" || s === "yes" || s === "1";
}

/* Tag normalization */
function tagKey(raw) {
  return String(raw ?? "")
    .toLowerCase()
    .trim()
    .replace(/[-_]+/g, " ")
    .replace(/\s+/g, " ");
}
function tagSlugFromKey(key) {
  return String(key ?? "")
    .toLowerCase()
    .trim()
    .replace(/&/g, "and")
    .replace(/[^a-z0-9]+/g, "-")
    .replace(/(^-|-$)/g, "");
}
function tagLabelFromKey(key) {
  const s = String(key ?? "").trim().replace(/\s+/g, " ");
  if (!s) return "";
  return s
    .split(" ")
    .filter(Boolean)
    .map((w) => (w ? w.charAt(0).toUpperCase() + w.slice(1) : ""))
    .join(" ");
}

/* Safe date getter */
function getPostDate(post) {
  return (
    post?.data?.pubDate ??
    post?.data?.date ??
    post?.data?.published ??
    post?.data?.updated ??
    null
  );
}
function sortNewestFirst(a, b) {
  const da = new Date(getPostDate(a) ?? 0).getTime();
  const db = new Date(getPostDate(b) ?? 0).getTime();
  return db - da;
}

/* Hero image field */
function heroImage(post) {
  return String(post?.data?.heroImage ?? "").trim();
}

/* Related posts */
const RELATED_LIMIT = 3;
const MAX_FEATURED_IN_RELATED = 1;

function normalizeTags(post) {
  const raw = Array.isArray(post?.data?.tags) ? post.data.tags : [];
  const keys = raw.map(tagKey).filter(Boolean);
  return Array.from(new Set(keys));
}
function sharedTagScore(aKeys, bKeys) {
  if (!aKeys.length || !bKeys.length) return 0;
  const set = new Set(aKeys);
  let score = 0;
  for (const k of bKeys) if (set.has(k)) score++;
  return score;
}
function buildRelatedPosts(currentPost, allCityPosts) {
  const currentSlug = currentPost?.slug;
  const currentKeys = normalizeTags(currentPost);

  const candidates = allCityPosts
    .filter((p) => p?.slug && p.slug !== currentSlug)
    .map((p) => {
      const keys = normalizeTags(p);
      const score = sharedTagScore(currentKeys, keys);
      const date = new Date(getPostDate(p) ?? 0).getTime();
      return { p, score, date };
    });

  candidates.sort((x, y) => {
    if (y.score !== x.score) return y.score - x.score;
    return y.date - x.date;
  });

  const withOverlap = candidates.filter((c) => c.score > 0).map((c) => c.p);
  const withoutOverlap = candidates.filter((c) => c.score === 0).map((c) => c.p);
  const ranked = [...withOverlap, ...withoutOverlap];

  const featured = ranked.filter(isFeatured);
  const nonFeatured = ranked.filter((p) => !isFeatured(p));

  const result = [];
  result.push(...featured.slice(0, MAX_FEATURED_IN_RELATED));
  result.push(...nonFeatured.slice(0, RELATED_LIMIT - result.length));

  const remaining = ranked.filter((p) => !result.some((r) => r.slug === p.slug));
  result.push(...remaining.slice(0, RELATED_LIMIT - result.length));

  return result.slice(0, RELATED_LIMIT);
}

/* Build-safe getStaticPaths */
export async function getStaticPaths() {
  const posts = await getCollection("posts");

  const daytonaPosts = posts
    .filter((post) => {
      const city = String(post?.data?.city ?? "daytona").toLowerCase().trim();
      if (city === "daytona") return true;
      return typeof post?.id === "string" && post.id.startsWith("daytona/");
    })
    .sort((a, b) => {
      const da = new Date(
        a?.data?.pubDate ?? a?.data?.date ?? a?.data?.published ?? a?.data?.updated ?? 0
      ).getTime();
      const db = new Date(
        b?.data?.pubDate ?? b?.data?.date ?? b?.data?.published ?? b?.data?.updated ?? 0
      ).getTime();
      return db - da;
    });

  return daytonaPosts.map((post) => ({
    params: { slug: post.slug },
    props: { post },
  }));
}

/* Page data */
const { post } = Astro.props;
const title = post.data.title;
const subtitle = post.data.description ?? "GoDo Daytona post";
const { Content } = await post.render();

const all = await getCollection("posts");
const daytonaPosts = all
  .filter((p) => {
    const city = String(p?.data?.city ?? "daytona").toLowerCase().trim();
    if (city === "daytona") return true;
    return typeof p?.id === "string" && p.id.startsWith("daytona/");
  })
  .sort(sortNewestFirst);

// tags for this post
const rawTags = Array.isArray(post.data?.tags) ? post.data.tags : [];
const uniqueKeySet = new Set(rawTags.map(tagKey).filter(Boolean));
const tagItems = Array.from(uniqueKeySet).map((key) => ({
  key,
  label: tagLabelFromKey(key),
  slug: tagSlugFromKey(key),
}));

// related posts
const relatedAll = buildRelatedPosts(post, daytonaPosts);
const seen = new Set();
const deduped = relatedAll.filter((p) => {
  if (seen.has(p.slug)) return false;
  seen.add(p.slug);
  return true;
});
const relatedPosts = [
  ...deduped.filter(isFeatured),
  ...deduped.filter((p) => !isFeatured(p)),
].slice(0, 3);

// date display
const pub = getPostDate(post);
const pubText = pub ? new Date(pub).toLocaleDateString("en-US") : "";
const featured = isFeatured(post);

// hero image
const hero = heroImage(post);
---

<Base title={title} subtitle={subtitle} cityBase="daytona">
  <section class="section">
    <div class="container">
      <div class="postLayout">

        <!-- LEFT: article (75%) -->
        <div class="postMain">
          <div class="postWrap">
            ...all article content...
          </div>
        </div>

        <!-- RIGHT: sidebar (25%) -->
        <aside class="postSidebar">
          <h3 class="sectionTitle">Upcoming Events</h3>
          <!-- drop your events component here later -->
          <p style="color:var(--muted);font-size:14px">Events coming soon.</p>
        </aside>

      </div>
    </div>
  </section>
</Base>

      {/* TITLE / DECK / META FIRST */}
      <h1 class={`h1 postTitle ${featured ? "isFeatured" : ""}`}>{title}</h1>
      {post.data.description && <p class="lede">{post.data.description}</p>}

      <div class="metaRow">
        {pubText && <span class="metaDate">{pubText}</span>}

        {tagItems.length > 0 && (
          <span class="metaTags" aria-label="Topics">
            {tagItems.map((t) => (
              <a class="topicPill" href={`/daytona/topic/${t.slug}/`}>{t.label}</a>
            ))}
          </span>
        )}

        {featured && <span class="featuredPill">FEATURED</span>}
      </div>

      {/* HERO AFTER */}
      {hero && (
        <figure class="heroFigure">
          <img
            class="heroFull"
            src={hero}
            alt={post.data.title ?? "Post image"}
            loading="eager"
            decoding="async"
          />
        </figure>
      )}

      <article class="postBody">
        <Content />
      </article>

      {/* Related posts: keep as-is */}
      {relatedPosts.length > 0 && (
        <div class="relatedWrap">
          <h3 class="relatedHead">Related posts</h3>

          <div class="relatedList">
            {relatedPosts.map((p) => {
              const t = p.data.title ?? p.slug;
              const desc = p.data.description ?? "";
              const d = getPostDate(p);
              const dateText = d ? new Date(d).toLocaleDateString("en-US") : "";
              const img = heroImage(p);

              return (
                <a class="relatedRow" href={`/daytona/posts/${p.slug}/`}>
                  <div class="relatedThumb">
                    {img ? (
                      <img class="relatedImg" src={img} alt={t} loading="lazy" decoding="async" />
                    ) : (
                      <div class="relatedImg relatedFallback" aria-hidden="true"></div>
                    )}
                  </div>

                  <div class="relatedText">
                    <div class="relatedTitle">{t}</div>
                    {desc && <div class="relatedDesc">{desc}</div>}
                    {dateText && <div class="relatedMeta">{dateText}</div>}
                  </div>

                  {isFeatured(p) && <span class="featuredPill featuredPillSmall">FEATURED</span>}
                </a>
              );
            })}
          </div>
        </div>
      )}

      <p class="postLinks">
        <a class="pill" href="/daytona/posts/">← All posts</a>
        <a class="pill" href="/daytona/topics/">Topics</a>
      </p>

    </div>
  </section>
</Base>

<style>
  /* Keep layout logic intact; just presentation */
 .postWrap {
  max-width: 780px;
  margin-left: 0;        /* pin to left instead of centering */
  margin-right: 0;
  padding-left: 18px;
  padding-right: 18px;
}

  /* Featured title 15% larger on article page */
  .postTitle { margin: 0; }
  /* BIG editorial headline (2x) */
/* BIG editorial headline (slightly reduced) */
.postTitle {
  margin: 0;
  font-size: clamp(30px, 3.4vw, 46px); /* ~25% smaller */
  font-weight: 950;
  letter-spacing: -0.5px;
  line-height: 1.08;
}

/* Slightly smaller deck/description */
.lede {
  margin-top: 12px;
  font-size: clamp(16px, 1.8vw, 20px);
  line-height: 1.4;
  color: rgba(232,237,244,.88);
}


/* Bigger deck/description */
.lede {
  margin-top: 14px;
  font-size: clamp(18px, 2.2vw, 24px);
  line-height: 1.35;
  color: rgba(232,237,244,.88);
}

  /* HERO FIRST: 16:9 editorial hero */
  .heroFigure {
    margin: 0 0 14px 0;
  }
  .heroFull {
    display: block;
    width: 100%;
    aspect-ratio: 16 / 9;
    object-fit: cover;
    border-radius: 16px;
    border: 1px solid rgba(255,255,255,.10);
    background: rgba(255,255,255,.02);
    box-shadow: 0 10px 26px rgba(0,0,0,.30);
  }

  .postBody {
    margin-top: 18px;
    line-height: 1.65;
  }

 .metaRow{
  margin-top: 14px;
  margin-bottom: 18px; /* <-- adds space before hero image */
  display:flex;
  flex-wrap:wrap;
  align-items:center;
  gap:12px;
  opacity:.82;
}

  }
  .metaDate{ opacity:.75; }
  .metaTags{
    display:inline-flex;
    flex-wrap:wrap;
    gap:8px;
    align-items:center;
  }

  /* Smaller + faded yellow (~30% down) */
  .metaRow .topicPill{
    display:inline-flex !important;
    align-items:center !important;
    justify-content:center !important;
    padding: 5px 10px !important;
    border-radius: 999px !important;
    font-size: 11px !important;
    font-weight: 800 !important;
    letter-spacing: .02em !important;
    text-decoration: none !important;

    color: #111 !important;
    background: rgba(255, 220, 80, 0.65) !important;
    border: 1px solid rgba(255, 220, 80, 0.55) !important;

    transition: transform .08s ease, filter .12s ease, opacity .12s ease;
  }
  .metaRow .topicPill:hover { transform: translateY(-1px); filter: brightness(1.02); }
  .metaRow .topicPill:active { transform: translateY(0px); opacity: .92; }

  .featuredPill {
    font-size: 12px;
    font-weight: 800;
    letter-spacing: .04em;
    padding: 6px 10px;
    border-radius: 999px;
    border: 1px solid rgba(255,220,80,.65);
    color: rgba(255,220,80,.95);
    background: rgba(255,220,80,.12);
    white-space: nowrap;
  }
  .featuredPillSmall {
    font-size: 11px;
    padding: 5px 9px;
    align-self: start;
  }

  /* Related posts: vertical list like homepage feed */
  .relatedWrap {
    margin-top: 26px;
  }
  .relatedHead {
    margin: 0 0 10px 0;
    font-size: 16px;
    opacity: .9;
  }
  .relatedList {
    display: grid;
    gap: 12px;
  }
  .relatedRow {
    text-decoration: none;
    display: grid;
    grid-template-columns: 96px 1fr auto;
    gap: 12px;
    align-items: start;
    border: 1px solid rgba(255,255,255,.12);
    border-radius: 14px;
    padding: 12px 12px;
    background: rgba(255,255,255,.03);
    transition: transform 160ms ease, box-shadow 160ms ease, border-color 160ms ease;
  }
  .relatedRow:hover {
    transform: translateY(-1px);
    border-color: rgba(255,220,80,.22);
    box-shadow: 0 0 0 1px rgba(255,220,80,.16), 0 10px 26px rgba(0,0,0,.30);
  }
  .relatedThumb {
    width: 96px;
  }
  .relatedImg {
    width: 96px;
    height: 66px;
    border-radius: 12px;
    object-fit: cover;
    border: 1px solid rgba(255,255,255,.12);
    background: rgba(255,255,255,.02);
    display: block;
  }
  .relatedFallback {
    background:
      radial-gradient(160px 120px at 30% 25%, rgba(255,255,255,.08), rgba(255,255,255,0)),
      linear-gradient(180deg, rgba(255,255,255,.04), rgba(255,255,255,.02));
  }
  .relatedText { min-width: 0; }
  .relatedTitle {
    font-weight: 900;
    color: rgba(232,237,244,.95);
    line-height: 1.18;
    font-size: 15.5px;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .relatedDesc {
    margin-top: 6px;
    opacity: .82;
    color: rgba(232,237,244,.78);
    line-height: 1.35;
    font-size: 13.5px;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .relatedMeta {
    margin-top: 8px;
    opacity: .6;
    color: rgba(232,237,244,.65);
    font-size: 12.5px;
  }

  @media (max-width: 520px) {
    .relatedRow {
      grid-template-columns: 1fr;
      gap: 10px;
    }
    .relatedThumb { width: 100%; }
    .relatedImg { width: 100%; height: auto; aspect-ratio: 16 / 9; }
    .featuredPillSmall { justify-self: start; }
  }

  .postLinks {
    margin-top: 22px;
    display:flex;
    gap:10px;
    flex-wrap:wrap;
  }
.postLayout {
  display: grid;
  grid-template-columns: 1fr 280px;
  gap: 32px;
  align-items: start;
}

.postMain {
  min-width: 0;   /* prevents grid blowout */
}

.postWrap {
  max-width: 100%;
  padding-left: 0;
  padding-right: 0;
}

.postSidebar {
  position: sticky;
  top: 80px;       /* clears the sticky topbar */
  min-width: 0;
}

@media (max-width: 860px) {
  .postLayout {
    grid-template-columns: 1fr;
  }
  .postSidebar {
    position: static;
  }
</style>
