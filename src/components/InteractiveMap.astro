---
export interface MapLocation {
  label: string;
  address?: string;
  lat?: number;
  lng?: number;
}

export interface Props {
  locations: MapLocation[];
  height?: string;
}

const { locations = [], height = "360px" } = Astro.props;

/* Filter to only pins that have coordinates */
const pins = locations.filter((l) => l.lat != null && l.lng != null);

if (pins.length === 0) return;

/* Unique ID so multiple maps on a page don't collide */
const mapId = `imap-${Math.random().toString(36).slice(2, 8)}`;
---

<div class="imapWrap" style={`height: ${height}`}>
  <div id={mapId} class="imapEl"></div>
</div>

<!-- Leaflet CSS -->
<link
  rel="stylesheet"
  href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
  integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY="
  crossorigin=""
/>

<script define:vars={{ mapId, pins }}>
  /* Dynamically load Leaflet JS then initialise */
  (function () {
    function initMap() {
      /* ── Tile layer ── */
      const CartoDB = "https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png";
      const attribution =
        '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors &copy; <a href="https://carto.com/">CARTO</a>';

      const container = document.getElementById(mapId);
      if (!container || typeof window.L === "undefined") return;

      /* Prevent double-init if Astro view transitions replay the script */
      if (container._leaflet_id) return;

      const map = window.L.map(mapId, { scrollWheelZoom: false, zoomControl: true });

      window.L.tileLayer(CartoDB, {
        attribution,
        subdomains: "abcd",
        maxZoom: 19,
      }).addTo(map);

      /* ── Custom yellow pin ── */
      const yellowPin = window.L.divIcon({
        className: "",
        html: `<div class="imap-pin" aria-hidden="true"></div>`,
        iconSize: [22, 22],
        iconAnchor: [11, 11],
        popupAnchor: [0, -14],
      });

      /* ── Add markers ── */
      const bounds = [];
      pins.forEach((pin) => {
        const latlng = [pin.lat, pin.lng];
        bounds.push(latlng);

        const popupContent = pin.address
          ? `<strong>${pin.label}</strong><br><span style="font-size:12px;color:#555;">${pin.address}</span>`
          : `<strong>${pin.label}</strong>`;

        window.L.marker(latlng, { icon: yellowPin })
          .bindPopup(popupContent, { maxWidth: 220 })
          .addTo(map);
      });

      /* ── Fit map to show all pins ── */
      if (bounds.length === 1) {
        map.setView(bounds[0], 15);
      } else {
        map.fitBounds(bounds, { padding: [32, 32] });
      }
    }

    /* Load Leaflet JS if not already present */
    if (typeof window.L !== "undefined") {
      initMap();
    } else {
      const script = document.createElement("script");
      script.src = "https://unpkg.com/leaflet@1.9.4/dist/leaflet.js";
      script.integrity = "sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV/XN/sp38=";
      script.crossOrigin = "";
      script.onload = initMap;
      document.head.appendChild(script);
    }
  })();
</script>

<style>
  .imapWrap {
    width: 100%;
    border-radius: 14px;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.10);
    margin: 0 0 28px;
    position: relative;
  }

  .imapEl {
    width: 100%;
    height: 100%;
  }

  /* Yellow circular pin */
  :global(.imap-pin) {
    width: 22px;
    height: 22px;
    border-radius: 50%;
    background: #f9d900;
    border: 3px solid #000;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.35);
    transition: transform 120ms ease;
  }

  :global(.leaflet-marker-icon:hover .imap-pin) {
    transform: scale(1.25);
  }

  /* Popup polish */
  :global(.leaflet-popup-content-wrapper) {
    border-radius: 10px !important;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.18) !important;
    font-family: inherit !important;
    padding: 4px 2px !important;
  }

  :global(.leaflet-popup-content) {
    margin: 10px 14px !important;
    font-size: 13.5px !important;
    line-height: 1.45 !important;
  }

  :global(.leaflet-popup-tip) {
    box-shadow: none !important;
  }

  /* Hide Leaflet's default focus outline on the container */
  :global(.leaflet-container:focus) {
    outline: none;
  }
</style>
